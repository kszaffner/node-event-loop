# Knowledge Index

Status: **enabled**

Index-first: check this file for relevant topics before loading any file
below in full. See `.claude/skills/knowledge/SKILL.md` for the retrieval
and update workflow.

This project ships with the Knowledge Base initialized (empty beyond this
index) so `/knowledge update` has somewhere to write to immediately.
Delete `.claude/knowledge/` entirely if you don't want to use this system
— the rest of the workflow (`code-review`) works without it.

## Topics

_No topics recorded yet. Run `/knowledge update` after a scenario reveals
a non-obvious WHY (e.g. platform-dependent ordering, a Node-version
quirk) — not just WHAT the scenario shows._

<!--
Example entry once you have real content:

- [dns-lookup-vs-resolve.md](decisions/dns-lookup-vs-resolve.md) — why
  dns.lookup() goes through the thread pool but dns.resolve() doesn't
-->
